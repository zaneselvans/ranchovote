"""Textual-based entry points for terminal trace exploration.

The TUI package mirrors the web package: it consumes the shared trace service and keeps
terminal presentation concerns separate from storage and counting logic.
"""
